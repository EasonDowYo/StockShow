@model IEnumerable<StockShow.Models.StockTypeTable>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>List</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StockType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StockType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StockTypeIndex">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StockTypeIndex">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StockTypeIndex">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
